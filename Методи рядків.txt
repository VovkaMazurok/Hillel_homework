# довжина рядка
len(str)

# перетворення рядка до нижнього регістру
str.lower()

# перетворення рядка до верхнього регістру
str.upper()

# перевірка чи рядок складається лише з алфавітних символів
str.isalpha()

# перевірка чи рядок складається лише з цифр
str.isdigit()

# перевірка чи рядок складається лише з символів у нижньому регістрі
str.islower()

# перевірка чи рядок складається лише з символів у верхньому регістрі
str.isupper()

# перевірка чи рядок складається лише з пробілів
str.isspace()

# видалення пробілів з початку та кінця рядка
str.strip()

# видалення пробілів з початку рядка
str.lstrip()

# видалення пробілів з кінця рядка
str.rstrip()

# заміна підрядка в рядку
str.replace(old, new)

# поділ рядка на список за заданим роздільником
str.split(separator)

# об'єднання списку рядків у єдиний рядок з роздільником
separator.join(list)

# знаходження першої позиції підрядка в рядку
str.find(sub)

# форматування рядка за допомогою форматування
str.format(values)
